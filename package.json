{
  "name": "melange-pancake",
  "version": "1.0.0",
  "license": "BSD-3-Clause",
  "scripts": {
    "test": "jest --config",
    "tdd": "jest --watchAll"
  },
  "devDependencies": {
    "jest": "^26.5.2"
  }
}
